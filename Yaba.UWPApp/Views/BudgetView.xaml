<Page
    x:Class="Yaba.UWPApp.Views.BudgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yaba.UWPApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="60" />
            <RowDefinition Height="60"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" FontSize="30" Text="{x:Bind _vm.Name}"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Recurring total"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="{x:Bind _vm.RecurringTotal}" TextAlignment="Right"/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="Categories: "/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="{x:Bind _vm.CategoriesTotal}" TextAlignment="Right"/>
        <StackPanel Grid.Column="0" Grid.Row="3">
            
        </StackPanel>
    </Grid>
</Page>
